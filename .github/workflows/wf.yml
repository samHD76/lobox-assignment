name: Build and Push CI/CD

on:
  push:
    branches:
      - '*'
jobs:
  build-Push:
     runs-on: ubuntu-latest
     steps:
        - name: Log in to Docker Hub
          uses: docker/login-action@v3
          with:
            username: ${{ secrets.DOCKERHUB_USERNAME }}
            password: ${{ secrets.DOCKERHUB_PAT }}

        - uses: actions/checkout@v3
        - name: build and push image
          run: |
            docker build -f devops/Dockerfile -t samhdabd/lobox-assignment:${{ github.ref_name }}-${{ github.sha }} .
            docker push samhdabd/lobox-assignment:${{ github.ref_name }}-${{ github.sha }}

#        - name: Deploy to {{ENVIRONEMNT}}
#          uses: appleboy/ssh-action@v1
#          with:
#            host: ${{secrets.{{ENVIRONEMNT}}_SERVER_IP}}
#            username: gitops
#            key: "${{secrets.{{ENVIRONEMNT}}_SERVER_KEY}}"
#            port: ${{ secrets.YOUR_SERVER_SSH_PORT || '22' }}
#            script: |
#                {get from company's secret manger }
#                docker-compose up -d